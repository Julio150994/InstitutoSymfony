{% extends 'base.html.twig' %}

{% block title %}Formulario para añadir alumnos{% endblock %}

{% block bootstrap %}
<!-- CSS Bootstrap 5 -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">

<!-- FontAwesome 6.2.0 icons -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.0/css/all.min.css">
{% endblock %}

{% block alumnos_css %}
<!-- Archivo CSS en Symfony desde carpeta public -->
<link rel="stylesheet" href="{{ asset('css/alumnos.css') }}">
{% endblock %}


{% block aniadir_alumno %}

<div class="example-wrapper rounded mt-xs-2 mt-lg-5 mt-md-5 p-xs-2 p-lg-5 p-md-5
    pb-xs-2 pb-lg-3 pb-md-3 container-background justify-content-start">
	<h2 class="text-center text-secondary pt-xs-2 pt-lg-4 pt-md-4" style="font-family: helvetica;">
        {{ titulo_formulario }}
    </h2>

    <div class="container-fluid mt-xs-3 mt-lg-5 mt-md-5 text-dark">
        
        {{ form_start(form) }}
        <div class="form-group ">
            {{ form_row(form.nombre, {'attr': {'class': 'form-control'}}) }}
        </div>
        <div class="form-group">
            {{ form_row(form.apellidos, {'attr': {'class': 'form-control'}}) }}
        </div>

        <div class="form-group">
            {{ form_row(form.edad, {'attr': {'class': 'form-control'}}) }}
        </div>

        {# Para el diseño del botón de subir imágen #}
        <div class="form-group">
            {{ form_row(form.foto, {'attr': {'class': 'form-control'}}) }}
        </div>

        <div class="form-group">
            {{ form_row(form.preciomatricula, {'attr': {'class': 'form-control', 'step': '0.01'}}) }}
        </div>

        <div class="row row-cols-2 mt-xs-1 mt-lg-3 mt-md-3">
            <a href="{{ path('alumnos') }}" class="btn btn-danger text-white">
                <span class="fas fa-solid fa-arrow-left"></span>
                <span>Cancelar</span>
            </a>

            <button class="btn {{ button_color|default('btn-success') }}  mt-3">
                <span class="fas fa-solid fa-plus"></span>
                <span>{{ button_label|default('Añadir') }}</span>
            </button>
        </div>
    </div>
</div>
{% endblock %}